# 文档缺口

- `cover-p-a` 与 `cover-p-b` 的 postdoc 细节仍需在完成 bbox parity 后写入
  `reference/cover-layout-contracts.md`。
- `01-title-page-en` 尚未导入 `thuthesis3`，导入后需要更新
  `reference/cover-oracles-and-fixtures.md` 和 `reference/cover-migration-status.md`。
- spine 与 declaration-page public API 决策完成后，需要把最终契约从
  `reference/cover-migration-status.md` 提升到 architecture/reference 文档。
