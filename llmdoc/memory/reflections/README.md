# Reflections

This directory stores chronological working reflections from future tasks.

Use reflections for lessons learned during difficult migrations, failed tests,
user corrections, and documentation gaps. Promote only durable knowledge into
stable docs such as `must/`, `architecture/`, `guides/`, and `reference/`.
