# ThuThesis3

**激情施工中**。

利用 LaTeX3 接口重构的 `thuthesis` 文档类，暂不可用。在实现原有文档类的全部功能前不接受 Pull requests。

如果希望预览目前的进度，可以在本地安装 TeX Live 2022 后，克隆仓库并运行 `l3build install` 安装开发版，随后使用 `xelatex` 编译 `docs/thuthesis-example.tex`。
